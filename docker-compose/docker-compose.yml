version: '2'

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - '../data:/var/lib/postgresql'
    ports:
      - '5432:5432'